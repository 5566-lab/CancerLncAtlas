# V3.2 execution progress (2026-09-05)

## Frozen decisions

- Single-cell coverage is closed at 23 formal cancers plus 10 explicit
  typed-unavailable cancers. The failed rescue reasons are recorded in
  `V32_SINGLE_CELL_SCOPE_CLOSED_20260904.md`; this is not a recurring blocker.
- The local-CNV confounding audit and independent CNV OOF remain the current
  audited auxiliary results. They do not silently redefine the primary score.
- All CPU preparation, hashing, and training work is restricted to host `149`.

## Current gate

- Corrected G0/G1/G2 input root remains the authority:
  `./data/CancerLncAtlas/inputs/v32_g012_local_cnv_formal_prepared_20260903_r1`.
- The bounded SHA rebind worker on 149 completed all fifteen records in
  `g012_rebind_20260904_r5`.  Its immutable success receipt has SHA-256
  `bba5951ccae1eb493de7805abed357661f93a41c8609485e2169361d79c1e153`, and
  `REBOUND_INPUT_MANIFEST.json` has SHA-256
  `cc3f02c6f8e77f5079de1130d853ebb389f62097faf3dc45fc46c0e6adbcb793`.
  This was a read-only byte-identity/lineage closure over the existing
  corrected `.pt` files, not a second materialization.
- No G0/G1/G2 optimizer is running. The CompShare RTX 4090 is stopped and
  must remain stopped until input/code/launch hashes and the no-GPU preflight
  all pass.

- The first blocked, non-authorizing candidate at
  `g012_auth_gate_20260905_r1/receipt/` remains immutable history; it records
  `training_approval_emitted=false` and `launch_permitted=false` and cannot
  start a job. A separate current no-GPU static authorization was subsequently
  materialized only after the corrected code/config/decision binding passed.
- Current static authorization:
  `./data/CancerLncAtlas/runtime/tools/v32_g012_corrected_paid_gpu_20260905_r1/bootstrap/STATIC_AUTH_READY.json`
  (SHA-256
  `08ed7192d4ae7236670d50c5605bf74fe419b09b5ad5cfcc70509c3c859f0346`).
  It covers G0/G1/G2 × five folds, but is not evidence that a GPU or optimizer
  has started; the provider/runtime preflight is still intentionally pending.

### Why a one-time rebind was required

The corrected materialization is already valid and is not being rebuilt. Its
small authority receipts bind the patient/fold/graph inputs, but do not carry
an independent SHA-256 for each large `.pt` payload. The one-time rebind adds
those fifteen payload identities and confirms that each file is still the
same local-CNV-corrected object before a training launcher is allowed to read
it. It writes only small manifests under `g012_rebind_20260904_r5`; it does
not copy, rewrite, or duplicate the 182-GB input root. The deleted
2026-08-30 materialization was pre-local-CNV and cannot substitute for this
corrected input or its rebind.

This is a closed one-time gate: do not launch another rebind or regenerate the
15 payloads unless a payload changes or an independent integrity check reports
drift. The old baseline references that remain in immutable historical receipts
are provenance only and must not be interpreted as active inputs.

## Website

- The active isolated r9 candidate on host 149 now passes app construction,
  scope/catalog pair validation, and finite route smoke; r8 and earlier
  candidates are immutable diagnostic history.
- The r9 catalog remains fail-closed (`release_ready=false`,
  `production_deployed=false`): seven sidecars are hash-pinned, 32 manifest
  bindings remain pending, and 22 canonical capability statuses remain
  pending.  No mounted overlay is silently promoted to a primary route.
- Production promotion still requires a complete self-contained staging
  closure, corrected-input binding, fair model comparison, winner lock,
  sealed-test evaluation, and final acceptance.

## Storage layout

- The proposed shared `BASE/` + `DELTA_G0/DELTA_G1/DELTA_G2/` layout is not
  materialized. The fifteen **corrected local-CNV** monolithic payloads remain
  untouched authority;
  no loader switch or deletion is permitted before semantic-equivalence tests.

## Superseded pre-local-CNV payload deletion (2026-09-05)

- The explicitly superseded payload subdirectory
  `./data/CancerLncAtlas/inputs/v32_g012_patient_first_20260830_r1/formal_prepared_20260830_r3_affine_pyg280_localtorch`
  was deleted after a bounded no-process/no-target-handle check, at the user's
  direction. It was the historical pre-local-CNV materialization (40 files,
  63,355,101,926 bytes), not the parent graph-authority root.
- The parent root and its static graph/expression authorities remain because
  the corrected preparation references them. The corrected 15-payload root
  remains untouched. Deletion receipts on host 149:
  `OLD_PRE_LOCAL_CNV_INPUT_PREDELETE_20260905.json` and
  `OLD_PRE_LOCAL_CNV_INPUT_DELETE_20260905.json` (delete receipt SHA-256
  `be35e9675a1a252569d1876210fd38bfa794ecd3f40e9d867a7fdc4aaf730957`).

## Superseded transfer archive cleanup (2026-09-05)

The matching 63,355,166,720-byte transfer archive was also deleted on host
149 because it contained the same pre-local-CNV baseline payload:

`./data/CancerLncAtlas/exports/v32_g012_paid_gpu_20260831_r1/v32_g012_fresh_prepared_inputs_20260831_r1.tar`

Its pre-delete SHA-256 was
`1c17b7be89621e5125c39e87f05ce14beb1f2227afdb481d2e9a20049b56bab0`.
The small `.sha256` and `.bytes` provenance markers remain; the corrected
local-CNV input root was not touched. The server receipt is
`runtime/tools/v32_g012_corrected_paid_gpu_20260905_r1/OLD_BASELINE_ARCHIVE_DELETE_20260905.json`
(SHA-256
`51d3e6fd588ad6bf3fb48ab20b71424ba255c91aef076821b438166825406ecd`).

## Isolated website r9 closure (2026-09-05)

The r9 candidate is the current staging artifact.  It is an isolated repair
of r8's nested scope/catalog hash drift; r8 remains immutable diagnostic
history and is not active:

- Root: `./data/CancerLncAtlas/runtime/web_candidates/v32_20260905_r9`.
- The r9 runtime is self-contained under its code root; its active manifest
  has 39 bindings, exactly 7 `MOUNTED_HASH_PINNED` and 32 explicit
  `PENDING_FORMAL_SUCCESS` entries.
- Unified manifest SHA-256:
  `56c8228e04a051df4371eae43fc2eab45a9e3575ba5cbbed226a2ec2a553e485`.
  Catalog SHA-256:
  `f02bc1ba1b064a81dbd07a554fe94eef7d1b28c81f83b386ffcef24b902d62c1`;
  frontend catalog is byte-identical.  Capability-scope SHA-256:
  `9d80c4f4c9bcd58bdbb77b95a2255ea5269c3bf6169dabd790acc3e7100d913c`.
- The finite loopback service on 8303 passed 18/18 strict semantic route
  probes plus the overlay/download probes, including formal23 single-cell,
  explicit typed-unavailable behavior, directional CNV, and mixed
  lncRNA/protein exact-pathway queries.  Route-smoke receipt SHA-256:
  `d764edbf6b1cd844fa9c90c368334a7335761ba6cd6493a572931a9dc3852e10`.
- `create_app` constructed 108 routes successfully.  The service and runner
  were stopped after the smoke; port 8261 was not touched.  The independent
  audit records `release_ready=false`, `production_deployed=false`,
  `training_started=false`, and `gpu_started=false` explicitly.
- Scope/catalog pair audit SHA-256:
  `af02063f37fb890ad9560d6285dfa3a6671f346fe0a92d5737912dbdfa5bf31d`;
  external pair receipt SHA-256:
  `213cb63c6616d43897e832e61693641d865507808ebe131fa16df47c0baa6e4b`.
- Independent release audit:
  `r9/receipts/R9_INDEPENDENT_RELEASE_AUDIT_20260905.json`, SHA-256
  `4f48511e3197e8cdaebfe0cf8c4fb23fd959d2bd307e80477af4b2b80d874f32`,
  status `PASS_R9_STAGING_WITH_EXPLICIT_GAPS`.  The dedicated r9 launcher
  (pair-audit and host/port guard) is SHA-256
  `53be24b859e77042fba94283980e93d6380709fb44985409910a4f8528232f0a`.
- The remaining 22 pending bindings are intentionally fail-closed; the
  seven mounted sidecars are not promoted to canonical primary-model routes.
  Primary training, candidate comparison, winner-lock, sealed-test, and
  production release remain separate gates.

## Objective completion audit anchor (2026-09-05)

The requirement-by-requirement read-only audit is recorded in
`docs/V32_OBJECTIVE_COMPLETION_AUDIT_20260905.md` (SHA-256
`6ddeb583e2893ac807d6e37c8fbe09f47413756e30de46b9ccc074ed51627717`).
The single-cell scope-closure document was updated to reference the active
r9 staging state and has
SHA-256 `c15c77b7d3be44b058cc1a94f0daa060f3db091152f46dab3d69de179ad66fb6`.
These are documentation anchors only; they do not promote r9 to production
or reopen single-cell rescue.

The bounded server-side pending-binding inventory is recorded at
`./data/CancerLncAtlas/runtime/web_candidates/v32_20260905_r9/receipts/R9_PENDING_BINDING_INVENTORY_20260905.json`
(SHA-256
`d7becfc03143c2ad4a43c6100be19443acb22d489bba6f2e96c4eecaa0ab9617`).
It confirms 32 pending entries and zero safely closable entries.  The check
was manifest-only: it did not scan production or sealed-test data and did not
promote historical lineage.

## Documentation amendment (2026-09-05)

An earlier static-authorization paragraph contains a mojibake character in
the fold-coverage phrase.  The intended statement is that the authorization
covers all five folds for each of G0, G1, and G2.  This amendment is
documentation-only and does not change any authorization, input hash, or
training state.
